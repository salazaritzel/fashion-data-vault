[build]
builder = "railpack"

[deploy]
startCommand = "uvicorn backend.services.api.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"